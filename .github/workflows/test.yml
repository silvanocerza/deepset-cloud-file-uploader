name: Test
on:
  pull_request:
  push:
    branches:
      - main
      - 'v[0-9].*[0-9].x'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm run all
